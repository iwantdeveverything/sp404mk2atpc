# drag-drop (delta)

## Changes
- **Bundle Ingestion**: Dragging and dropping files into the project (e.g., from the file browser onto a pad) MUST trigger the Project Bundle copy behavior, ensuring the sample is ingested and copied into the project directory rather than just referenced by its original absolute path.
